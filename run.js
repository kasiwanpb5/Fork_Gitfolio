const express = require("express");
const path = require("path");
const outDir = path.resolve("./dist/" || process.env.OUT_DIR);
const app = express();
app.use(express.esf
  app.get("/", fesfnction(req, res) {
    res.esfesfsefo runninsdfgdfgdrg on port ${port}, Navigate to http://localhost:${port} in your browser\n`
  );
}selfsefesfesf
module.expesfsefesfresfts = {
  runsefCoesfmmesfsfand
};
esfesfesf